<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:fab="clr-namespace:Android.Widget;assembly=Xamarin.Android.Support.Design;targetPlatform=Android"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="VFood.Views.Entregadores">

    <ContentPage.Content>
        <StackLayout>
            <ListView ItemsSource="{Binding ListaEntregadores}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Orientation="Vertical">
                                <Label Text="{Binding Nome}" Style="{DynamicResource ListItemTextStyle}"/>
                                <Label Text="{Binding Telefone}" Style="{DynamicResource ListItemDetailStyle}"/>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <!--<Button Text="Adicionar" Clicked="{Binding AdicionarCommand}"/>-->
        </StackLayout>
    </ContentPage.Content>
</ContentPage>